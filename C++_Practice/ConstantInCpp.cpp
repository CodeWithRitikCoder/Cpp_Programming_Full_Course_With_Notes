/* Code a software to use of constand in C++ with RitikCoder. */
#include<iostream>
#include<conio.h>
using namespace std;

    int main(){
        system("color a");
            
            /* **** constand in C++ with RitikCoder ****. */
            const int a= 5;
            cout<<"The value of A before Apply constand keyword = "<<a<<endl;
            // a= 10;//This will through an error because of A is constant here.
            cout<<"The value of A before Apply constand keyword = "<<a<<endl;


    //getch();
    return (0);
    }
